using UnityEngine;
using System.Collections;

public class resolution : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Screen.SetResolution (800, 600, true); // true : full
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
